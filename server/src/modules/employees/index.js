const routes = require('./employees.routes');
const controller = require('./employees.controller');
const service = require('./employees.service');
const repository = require('./employees.repository');

module.exports = routes;
