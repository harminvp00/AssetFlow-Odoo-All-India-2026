const routes = require('./transfers.routes');
const controller = require('./transfers.controller');
const service = require('./transfers.service');
const repository = require('./transfers.repository');

module.exports = routes;
