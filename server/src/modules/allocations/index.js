const routes = require('./allocations.routes');
const controller = require('./allocations.controller');
const service = require('./allocations.service');
const repository = require('./allocations.repository');

module.exports = routes;
